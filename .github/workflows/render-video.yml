name: Render SKILLPIPS Cinematic Ad

on:
  push:
    branches:
      - gh-pages
      - claude/skillpips-cinematic-ad-z75Va
    paths:
      - "remotion/**"
  workflow_dispatch: # allow manual trigger from GitHub UI

jobs:
  render:
    name: Render MP4 (1080×1920 · 60fps · 45s)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: remotion/package-lock.json

      - name: Install ffmpeg
        run: sudo apt-get install -y ffmpeg

      - name: Install dependencies
        working-directory: remotion
        run: npm ci

      - name: Render video
        working-directory: remotion
        run: |
          mkdir -p out
          npx remotion render SkillpipsAd out/skillpips-ad.mp4 \
            --codec=h264 \
            --jpeg-quality=95 \
            --overwrite \
            --log=verbose

      - name: Upload MP4 artifact
        uses: actions/upload-artifact@v4
        with:
          name: skillpips-ad-mp4
          path: remotion/out/skillpips-ad.mp4
          retention-days: 30
